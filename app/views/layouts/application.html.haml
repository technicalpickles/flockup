!!! Transitional
%html
  %head
    %title= "FlockUp #{':' if yield(:title)} #{yield(:title)}"
    = stylesheet_link_tag 'blueprint/screen', :media => 'screen'
    = stylesheet_link_tag 'blueprint/print', :media => 'print'
    = "<!--[if IE]>#{stylesheet_link_tag 'blueprint/ie', :media => 'screen'}<![endif]-->"
    = stylesheet_link_tag 'all'
    = stylesheet_link_tag 'screen', :media => 'screen'
    = stylesheet_link_tag 'print', :media => 'print'
    - unless yield(:atom_feed_link).blank?
      = auto_discovery_link_tag :atom, yield(:atom_feed_link)
  %body
    .container
      #c-d.notice
        %strong 1-12-2009:
        Dearest FlockUp users,
        <a href="http://flock.com">Flock</a> has served us a cease & desist letter regarding their 'Flock' trademark.  In order to avoid customer, we will be rebranding and changing domains in the very near future. At the latest, this will occur by February 13th, 2009. The curious can find the letter here:
        = link_to 'part 1', image_path('flockup-cd-1.png')
        = link_to 'part 2', image_path('flockup-cd-2.png')
      #logo{:class => 'column span-24 last'}
        %a{:href => root_path}
          = image_tag 'logo.png'
      #tabs{:class => 'tabs column span-24 last'}
        %ul{:id => 'nav'}
          = tab_for 'flocks'
          = tab_for 'flockers'
          = tab_for 'Search', new_search_path
        
      #content{:class => 'column append-1 prepend-1 span-22'}
        - flash.each do |key, value|
          %div{:class => key}= h(value)
        = yield
      #footer{:class => 'column span-24'}
        = link_to 'About', about_path
        |
        = link_to 'Home', root_path
        |
        = link_to 'Tweet Us!', 'http://twitter.com/flockup'
